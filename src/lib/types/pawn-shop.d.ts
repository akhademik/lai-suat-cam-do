export type TheAsset = {
  pawn_date: Date;
  pawn_money: string;
  total_days: number;
  redemption_date: Date;
};

export type TheAssetItem = {
  id: string;
  pawn_money: string;
  total_days: number;
};
